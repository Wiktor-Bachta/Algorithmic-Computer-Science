from setuptools import find_packages, setup

setup(
    name='libpy',
    packages=find_packages(include=['libpy']),
    version='0.1.0',
    description='JPP assignment in Python',
    author='Wiktor Bachta',
    install_requires=[],
)